# Policy Sketch

> **This is a SKETCH, not a trained policy.** No training was run. This document
> describes how imitation learning *would* fit into the pipeline if pursued.

## Approach
TODO: one-line description of the imitation-learning approach (behavior cloning on retargeted G1 trajectories).

## Inputs / outputs
TODO: one-line description of policy inputs (observations) and outputs (G1 joint targets).

## Why this method
TODO: one-line justification for choosing this method given time/data constraints.

## How we'd validate
TODO: one-line description of how we'd validate the policy (e.g. sim replay vs. demo).

## What we did NOT train
TODO: one-line note that no policy was trained — this is a sketch only.
